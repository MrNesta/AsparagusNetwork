@{
    ViewBag.Title = "EatenConfirm";
}

<h2>Потверждение употребленья спаржи</h2>

<div class="row">
    @using (Html.BeginForm("EatenConfirm","Account"))
    {
        <div class="form-horizontal">
            <p class="text-info">
                Вы успешно прошли проверку подлинности через <strong>@ViewBag.LoginProvider</strong>
                Если вы наконец-то попробывали спаржу, отменьте галочкой, если нет снимите галочку.
            </p>
            <div class="form-group">
                @Html.Label("Ели лы вы спаржу", htmlAttributes: new { @class = "control-label col-md-2" })
                <div class="col-md-10">
                    @Html.CheckBox("eaten", new { htmlAttributes = new { @class = "form-control col-md-6" } })
                </div>
            </div>

            <div class="form-group">
                <div class="col-md-offset-2 col-md-10">
                        <input type="submit" name="loginType" value="Отправить" class="btn btn-default" />
                </div>
            </div>
        </div>
    }
</div>